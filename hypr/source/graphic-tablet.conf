device {
    name = wacom-one-by-wacom-s-pen
    # 'current' follows your mouse, but 'DP-1' (or your main monitor) 
    # is often more stable for drawing.
    output = current

    # Absolute input is standard for drawing
    relative_input = false
}
